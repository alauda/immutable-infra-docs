---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: dcsiphostnamepools.infrastructure.cluster.x-k8s.io
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: DCSIpHostnamePool
    listKind: DCSIpHostnamePoolList
    plural: dcsiphostnamepools
    singular: dcsiphostnamepool
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: DCSIpHostnamePool is the Schema for the dcsiphostnamepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DCSIpHostnamePoolSpec defines the desired state of DCSIpHostnamePool.
            properties:
              pool:
                items:
                  properties:
                    dns:
                      type: string
                    gateway:
                      type: string
                    hostname:
                      type: string
                    ip:
                      type: string
                    ipv6:
                      type: string
                    machineName:
                      type: string
                    mask:
                      type: string
                  type: object
                type: array
            required:
            - pool
            type: object
          status:
            description: DCSIpHostnamePoolStatus defines the observed state of DCSIpHostnamePool.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
